cctally codex quota: breakdown requires selectors resolving to exactly one quota identity; candidates:
  root-key=root-a limit-key=limit-primary
  root-key=root-a limit-key=limit-secondary
  root-key=root-b limit-key=limit-primary
