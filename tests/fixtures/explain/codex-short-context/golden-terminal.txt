Window 2026-08-10T00:00:00Z .. 2026-08-17T00:00:00Z [Etc/UTC] (half-open: the start is included, the end is not)
Measured at 2026-08-17T00:00:00Z
This explains locally retained cost and tokens, not quota percentage.
The classes overlap — one session is also a project and a model — so shares do not sum to 100 percent.
A subject is reported as a contributor at a share of 0.20 or more of its named denominator.

Rules in force, by class:
  Prompt-cache churn: Turns whose prompt cache was rebuilt: the cached prefix collapsed and was re-created in the same turn.
  Short conversations carrying large context: Conversations with at most 3 human turns where at least one request used at least 80% of that request's model context window. With several replies the largest single request fraction is used, never their sum.
  Subagent fan-out: Cost attributable to delegated subagent work, grouped by parent. On Codex this is an identifiable subset of unknown completeness.
  Turn definition: main-thread human messages only — non-sidechain, non-subagent, after command and meta normalisation; assistant replies associate with the nearest preceding human.

== codex · all accounts ==
Denominator totalExplainedRetainedCost: $3.44 of locally retained cost

Reported contributors, ranked by observed cost:
  1. [One session dominating] session-1 — $3.02, 87.8% of the denominator (confidence high)
       baseline: withheld (baseline_insufficient)
       coverage: support 22 units, cost coverage 100%, identity coverage 100%, priced without fallback 100%, window retained 100%
       -> Run cctally codex session --since 2026-08-10 --until 2026-08-17
  2. [Short conversations carrying large context] Short conversations carrying large context — $3.02, 87.8% of the denominator (confidence high, identifiable_subset_unknown_completeness)
       baseline: withheld (baseline_insufficient)
       coverage: support 22 units, cost coverage 100%, identity coverage 100%, priced without fallback 100%, window retained 100%, decidable 100%
       evidence: qualifying conversations 1, largest request as a share of its context window 90.0%, median human turns 2
       -> Run cctally explain --source codex --start-at 2026-08-10T00:00:00Z --end-at 2026-08-17T00:00:00Z --json

Classes reporting no contributor:
  Subagent fan-out (support 22 units, cost coverage 100%, identity coverage 100%, priced without fallback 100%, window retained 100%, decidable 100%, confidence high)

Classes with no measurement to report:
  Expensive model mix — insufficient_population (support 22 units, fewer than 2 distinct models)
  One project dominating — insufficient_population (support 22 units, fewer than 2 distinct projects)
  Concentrated 5-hour bursts — insufficient_population (support 12 units, fewer than 2 distinct blocks)

Classes this provider does not support:
  Prompt-cache churn — not applicable to codex

Verdict for codex: contributor_detected [3 of 6 applicable classes withheld]

Overall: contributor_detected [3 of 6 applicable classes withheld]
