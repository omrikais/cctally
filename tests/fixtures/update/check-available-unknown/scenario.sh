# `update --check` against an unknown install (source, dev symlink, etc.).
# No remote check is performed; latest defaults to current and the manual
# fallback line is emitted by the human renderer.
INSTALL_METHOD=unknown
CCTALLY_VERSION=1.4.0
