STDIN_JSON='{"hook_event_name":"PostToolBatch","session_id":"00000000-pppp"}'
THROTTLE_AGE_S="60"
# Non-JSON sentinel forces the mock OAuth refresh helper into the
# json.loads ValueError branch → returns "err(parse)" (mirrors the
# real RefreshUsageMalformedError code path at bin/cctally:13362).
MOCK_OAUTH="not-valid-json{"
