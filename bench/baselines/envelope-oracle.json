{
  "schemaVersion": 1,
  "_note": "HASHES ARE CORPUS-SPECIFIC. This oracle describes the envelope published over the GENERATED bench corpus named below, and nothing else. It does NOT replace, reconcile or re-take either historical per-store envelope reference \u2014 the #566 one recorded in docs/dashboard-gotchas.md and the #582 one in docs/backend-performance.md section 7. Those were captured against the maintainer's private store and their values are deliberately not restated here, because this file is published to the public mirror and one of those documents is private. Comparing a value here against a value there is meaningless.",
  "capture": {
    "command": "bin/cctally-snapshot-measure --corpus small --envelope <path>",
    "corpus_scale": "small",
    "corpus_seed": 42,
    "corpus_root": "/tmp/cctally-oracle-small-seed42",
    "generator_version": 6,
    "frozen_now": "2026-01-07T00:00:00Z",
    "tz": "Etc/UTC"
  },
  "corpus": {
    "fingerprint": "6d5d8358e1765415c40da93f3644b66e64fe4a81a253809248bde641b3a9082d",
    "dataset_counts": {
      "sessions": 40,
      "entries": 4300,
      "messages": 8600,
      "codex_entries": 1500,
      "codex_files": 30,
      "quota_windows": 120
    }
  },
  "envelope": {
    "bytes": 164743,
    "stable_sha256": "ca63029af9bae0ce50072c0d9bef8d899b01422633ca889d30b0784e90181ad2",
    "stable_excludes": [
      "data_version",
      "doctor",
      "sync_activity.server_epoch"
    ],
    "raw_sha256_is_unassertable": true,
    "_raw_sha256_note": "A raw sha256 over the whole envelope is deliberately NOT recorded: it does not survive a corpus rebuild, and it differs between machines. bin/cctally-snapshot-measure still prints it, which is the right tool for a before/after comparison across a code change with one built corpus left in place.",
    "_codex_leg_is_asserted": "Every capture runs assert_snapshot_renders_the_codex_cycle over the measured snapshot, so a recorded envelope can never be the degraded short branch: availability ok, no codex_cycle_unavailable, a hero cycle with non-zero spend.",
    "_583_s3_retake": "#583 S3 moved BOTH values, and the corpus fingerprint deliberately did not: `sources.all.data.providers` stopped carrying the two provider data objects and now publishes null for both, and `source_schema_version` went 9 -> 10. The re-capture was reviewed rather than adopted. A structural difference between a capture taken with `bin/` at the branch point (af3886fcd) and one taken after contains exactly three entries \u2014 the version bump and the two nulled provider members \u2014 and nothing else, not even the three excluded field sets, because the tool freezes the clock and the corpus did not change. The pre-change capture was 300,411 bytes at stable_sha256 e733e922d30942f9eb0a1b4cde798f69a4903879eb6686f276e0a33186369421. NOTE that the values this file carried before the re-take (299,863 bytes, 9e9acd339e46bb4936555ae7e495c66d70f2bdce011b2d2e2c4ddfba58a59297) did NOT match that pre-change capture: the oracle had already drifted by 548 bytes against `main`, because nothing in the authoritative suite runs `--verify` and a session between #583 S1 and S3 moved the envelope without re-taking it. The values recorded here are measured against the current tree."
  },
  "reproducibility": {
    "stable_sha256": "The value to assert. sha256 over the envelope with three named field sets removed and NOTHING else \u2014 `data_version` at the top level and inside each `sources[*]` entry, the top-level `doctor` object, and the nested `sync_activity.server_epoch`. All three sets are pinned against literals by test so none can grow without a stated reason, and a unit test proves the digest ignores exactly those and still discriminates other content \u2014 including each of the six remaining `sync_activity` fields, so a trim that quietly dropped the whole subtree would fail rather than pass.",
    "why_server_epoch_is_excluded": "#583 S2. It is a fresh uuid4 minted per SERVER PROCESS, so unlike the other two it moves on every run rather than across a corpus rebuild or between machines \u2014 leaving it in would break this oracle continuously instead of moving it once. It is a fixed 16 characters, so `envelope.bytes` stays a meaningful comparison and only the digest excludes it. It is the first NESTED exclusion, which is why it lives in its own set (`_PROCESS_VARIANT_PATHS`) rather than widening either flat tuple.",
    "why_data_version_is_excluded": "It is the snapshot signature, and it folds in the wall-clock metadata production ingest stamps. Two captures against one built corpus are byte-identical; after a full rebuild of the same corpus this is the only field that moves. That is also why bin/build-bench-fixtures.py defines its own reproducibility over semantic columns rather than bytes.",
    "why_doctor_is_excluded": "It is machine-derived: `counts` summarises 62 host checks and `fingerprint` is a fixed-length sha1 digest over the whole doctor result. Fixed length is why a second machine reproduces the corpus fingerprint and the envelope byte count EXACTLY while disagreeing on a hash over the bytes.",
    "verified_on": "Both LAN runners (different hosts), under two host timezones (Etc/UTC and Asia/Jerusalem), across a full corpus rebuild, and across two consecutive captures against one built corpus (byte-identical). The independence from `doctor` is proven by unit test rather than by a third machine, so this is a claim about what was measured and what the digest provably ignores, not that any machine anywhere reproduces it.",
    "tz": "LOAD-BEARING, and pinned by the tool rather than left to the caller. `display.tz` defaults to `local`, the envelope carries `display.resolved_tz`, and every date-bucketed panel buckets in it. Measured before the pin: the same command under Asia/Jerusalem produced 235,220 bytes against 232,328 under Etc/UTC. bin/cctally-snapshot-measure now sets TZ=Etc/UTC itself.",
    "corpus_root": "LOAD-BEARING. Codex identity is derived from the ABSOLUTE canonical provider-root path: `source_root_key` hashes it, and every Codex session key, project key, quota key and history key in the envelope derives from that. A corpus built under a different root publishes an envelope of identical LENGTH whose every Codex opaque key differs. `cctally-snapshot-measure` therefore defaults `--corpus-root` to a literal path rather than `tempfile.gettempdir()`. On macOS `/tmp` resolves to `/private/tmp`, so a capture is comparable within an OS family and not across one.",
    "environment": "The tool pins the corpus's own CLAUDE_CONFIG_DIR, CODEX_HOME and HOME for the measurement and RESTORES them on return, so it can be called as a library. That pinning is part of the capture: the envelope carries HOME/config-dependent keys \u2014 `transcriptsEnabled` appears or not depending on the transcript privacy gate \u2014 so a capture taken under a different home is a different artefact even with an identical corpus fingerprint. KNOWN NARROWING (#583 S1 review, recorded rather than fixed): the RE-TAKEN capture no longer discriminates `transcriptsEnabled`. The key is present-or-absent depending on the transcript privacy gate, and the corpus home the tool now pins resolves that gate to one state, so a change to it would not move this hash. The environment pinning is still part of the capture contract for every other home-dependent key; this one axis is simply no longer covered, and a gate that needs it must assert it directly rather than rely on this oracle.",
    "reader": "`bin/cctally-snapshot-measure --corpus small --verify` is the reader. It re-captures against this file and exits 3 on a mismatch of `stable_sha256`, `envelope.bytes` or `corpus.fingerprint`, reporting all three axes before returning. It compares `stable_sha256` and never the raw one, for the reason recorded above. The pytest estate deliberately does NOT own this check: the corpus root is part of the capture contract, so a pytest tmp directory reproduces a different hash by construction."
  }
}
